language: bash
services: docker

env:
  - PHP_BASE_IMAGE=7.1-fpm

install:
  - git clone https://github.com/yiisoft/yii2.git ~/build/yiisoft/yii2-docker/tests/yii2

before_script:
  - pwd

script:
  - docker-compose build

after_script:
  - docker images

# vim:set et ts=2 sw=2:
